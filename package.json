{
  "name": "tienda-puntos",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix backend start",
    "build": "npm --prefix backend install && npm --prefix backend run prisma:generate"
  },
  "engines": {
    "node": ">=18"
  }
}
